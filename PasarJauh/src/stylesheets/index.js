import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  formItemContainer: {
    flex: 1,
    borderColor: '#565656',
    borderWidth: 1,
    borderRadius: 5,
    margin: 5,
  },
});
